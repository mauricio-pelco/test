<?xml version="1.0" encoding="UTF-8" ?>
<Module>
  <ModulePrefs title="DDI Generator 5.1"
  description="Attempting to create a standalone JQL gadget" 
  author="Mauricio Munoz" 
  author_email="mauricio.pelco@gmail.com">
	<Require feature="dynamic-height"/>
    <Optional feature="gadget-directory">
      <Param name="categories">JIRA</Param>
    </Optional>
  </ModulePrefs>
  <Content type="html">
    <![CDATA[
		
		<div id="all" class="centered" style="display: block">
			<div id="search" style="display: block">  
				<a href="#" onclick="filterSelector()">Saved Filters</a>
			</div>
			<div id="updateButton" style="display: block">  
				<input id="update" onclick="filterValue()" type="button" value="UPDATE">
			</div>
			<div id="main" class="large" style="display: none">
			
			</div>
		</div>
		
		<input id="filter_filterId_id" onclick="filterValue()" type="hidden" name="filterId" value>
		
		
		<style type="text/css"> 
			.centered {  
			position:absolute;
			z-index: 100;  
			top:25%;  
			left:50%;  
			margin:-50px 0 0 -100px;  
			width:100px;  
			height:200px; 
			}
			
			.large {
			font-size:150px;
			color:red;
			} 
		
		</style>	
		
		<script type="text/javascript">
		
			function filterValue(){
				var convert = document.getElementById('filter_filterId_id').value;
				convert = parseInt(convert.replace(/[^0-9\.]/g, ''), 10);
				window.alert(convert);
			}
		
			function filterSelector(){
				var url =  "http://localhost:8080/secure/FilterPickerPopup.jspa?showProjects=false&field=filterId"
				var newWindow = window.open(url, "Select", "width=800, height=500, resizable, scrollbars=yes");
				newWindow.focus();
			}
		
			function showSection(section){
				var el = document.getElementById(section);
				el.style.display = "block";
				el.style.fontWeight = "bold";
			}
			
			function hideSection(section){
				var el = document.getElementById(section);
				el.style.display = "none";
			}
			
			function fetchData(){
					
				var params = {};
				var url = "http://localhost:8080/rest/api/2/search?jql=issueType=Bug";
//				url = url + escape("issuetype=Bug");
				
				params[gadgets.io.RequestParameters.CONTENT_TYPE] = gadgets.io.ContentType.JSON;
				
				gadgets.io.makeRequest(url, response, params);
				
			}
			
			function calculateDDI(R1, R2, R3){
				var DDI = 8*R1 + 4*R2 + R3;
				return(DDI);
			}
			
			function response(obj) { 
				var jsondata = obj.data;
				var R1 = 0;
				var R2 = 0;
				var R3 = 0;
				// Process returned JS object as an associative array
				for (var key in jsondata) {
					var value = jsondata[key];
					// If 'value' is an array, render its contents as a bulleted list
					if (value instanceof Array)
					{
						for (var i = 0; i < value.length ; i++)
						{
							var RPN = jsondata.issues[i].fields.customfield_10100;
							if(RPN >= 6 && RPN <= 9){
								R3++;
							}
							if(RPN >= 10 && RPN <= 17){
								R2++;
							}
							if(RPN >= 18 && RPN <= 25){
								R1++;
							}
						}
					}  
					// If 'value' isn't an array, just write it out as a string
					else {        
					
					}
				}
				var DDI = calculateDDI(R1, R2, R3);
				var nameNode = document.createTextNode(DDI);
				document.getElementById("main").appendChild(nameNode);
				document.getElementById("main").appendChild(document.createElement("br"));
				showSection('main');
			//	hideSection('drop-down');
				gadgets.window.adjustHeight();
			};
			
			gadgets.util.registerOnLoadHandler(fetchData);
			gadgets.window.adjustHeight();
		</script>
		
    ]]>
  </Content>
</Module>